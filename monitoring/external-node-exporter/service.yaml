apiVersion: v1
kind: Service
metadata:
  name: node-exporter-external
  namespace: monitoring
  labels:
    app: external-node-exporter
spec:
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
      protocol: TCP
