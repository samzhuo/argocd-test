{{- if and .Values.enabled .Values.tls.enabled -}}
{{-   if eq "true" (include "gitlab.gatewayApi.route.enabled" .) -}}
apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: {{ include "fullname" . | quote }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "gitlab.standardLabels" . | nindent 4 }}
    {{- include "gitlab.commonLabels" . | nindent 4 }}
spec:
  targetRefs:
  - group: ""
    kind: Service
    name: {{ include "registry.fullname" . | quote }}
  validation:
    {{- $serviceAddress := include "gitlab.assembleServiceAddress" (dict "name" (include "registry.fullname" .) "context" .) }}
    hostname: {{ .Values.backendTLSPolicy.hostname | default $serviceAddress | quote }}
    caCertificateRefs:
    - group: ""
      kind: Secret
      name: {{ .Values.tls.caSecretName | quote }}
{{-   end }}
{{- end }}
