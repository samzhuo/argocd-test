grafana:
  persistence:
    enabled: true
    storageClassName: local-path
    size: 5Gi

  ingress:
    enabled: true
    ingressClassName: traefik

    hosts:
      - grafana.lab.local

    path: /
    pathType: Prefix

prometheus:
  ingress:
    enabled: true
    ingressClassName: traefik

    hosts:
      - prometheus.lab.local

    path: /
    pathType: Prefix

  prometheusSpec:
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: local-path
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 20Gi

alertmanager:
  ingress:
    enabled: true
    ingressClassName: traefik

    hosts:
      - alertmanager.lab.local

    paths:
      - /
    pathType: Prefix

  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: local-path
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 5Gi